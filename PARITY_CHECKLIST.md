# Mobile ↔ Web Parity Checklist

One canonical list. Check a box in the same commit as the screen's rebuild.
Web repo: /Users/wesstarke/Desktop/v1portal. Mobile repo: this repo.
QA logins: athlete `thevarsityone@gmail.com` / `testpass123`, coach `thevarsityone+coach@gmail.com` / `CoachQA2026!`.

Format: `- [ ] mobile file ← web file — done means`

## Phase 0 — Tracking
- [x] Create this checklist, archive old audit docs to docs/archive/
- [x] Delete app/(tabs)/tracker.tsx and any drawer reference (dead web route) — confirmed deleted, no drawer reference remains

## Phase 1 — Nav drawers
- [x] components/AppDrawer.tsx ← components/DashboardShell.tsx — grouped nav sections, tier badge, pink-dot active indicator, no night-mode toggle. Verified live: athlete QA login, dashboard, drawer open, matches approved design.
- [x] components/CoachDrawer.tsx ← components/CoachShell.tsx — same, coach nav groups. Verified live: coach QA login, dashboard, drawer open, matches approved design.

## Phase 2 — Auth
- [x] app/(auth)/login.tsx ← app/(auth)/login/page.tsx — copy corrected (Log In not Sign In, DM Sans title), verified live
- [x] app/(auth)/forgot-password.tsx ← web forgot-password page — rebuilt centered-card pattern, verified live
- [x] app/(auth)/reset-password.tsx ← web reset-password page — rebuilt with strength meter + redirect bar, typechecks clean (not live-verified — requires a real recovery-link deep link to reach)
- [x] app/(auth)/signup/index.tsx ← app/(auth)/signup/page.tsx — verified live (step 0 entry + step 1 name), matches web exactly; Apple Sign In button is an intentional iOS-required addition not present on web
- [x] app/(auth)/signup/role.tsx ← app/(auth)/signup/role/page.tsx — verified live, matches web exactly including blurb copy and selection state

## Phase 3 — Athlete (tabs)
- [x] app/(tabs)/results.tsx ← app/dashboard/results/page.tsx — fully rebuilt (8 new components/results/* files), fixed real useAthleteScoreHistory bug (wrong column names/filter), verified live end-to-end incl. survey modal trigger
- [x] app/(tabs)/upgrade.tsx ← app/dashboard/upgrade/page.tsx — rebuilt copy/layout to match exactly (kept RevenueCat/StoreKit purchase flow, required on iOS instead of web's Stripe link), verified live via deep link
- [x] app/(tabs)/settings.tsx + settings/notifications.tsx ← app/dashboard/settings/page.tsx + notifications sub-page — fully rebuilt (Account, Invite Athlete, Email Notifications x3, Retake, Support, Feedback, Save, Danger Zone), added missing DB fields to useAthleteData, verified live
- [x] app/help.tsx ← app/dashboard/help/page.tsx — synced FAQ copy (10) and subject list (9) to exact web content, added FontFamily typography, verified live
- [x] app/(tabs)/messages/index.tsx + [conversationId].tsx ← app/dashboard/messages/page.tsx — list rebuilt to match exactly (title/subtitle, rounded-square avatars, green unread pill); kept mobile's own correctly-wired thread screen since web's own link to this list's threads is broken (points at the wrong table); verified live
- [x] app/(tabs)/calendar.tsx ← app/dashboard/calendar/page.tsx — already strong: same RECRUITING_DATES/milestones/category colors as web, working direct-table CRUD (web's own /api/events route doesn't exist, so web's custom events are actually broken); kept mobile's timeline view over web's grid (better fit for phone width); synced header copy
- [x] app/(tabs)/analytics.tsx ← app/dashboard/analytics/page.tsx — already a complete, faithful port (profile views chart, match activity, full recruiting-intelligence funnel/response-time/coach-momentum sections, exact copy) — no changes needed
- [x] app/(tabs)/edit-profile.tsx ← app/profile/edit/page.tsx — added the previously-deferred position-specific Season Stats editor (Playing Time varsity-years/first-varsity-grade pickers, This Season's Stats + one section per prior grade, all sourced from/written to assessments.responses exactly matching web's field ids), School Classification (state-aware via state_classifications table, falls back to generic letter/division options), and the "haven't taken SAT/ACT" toggle; also fixed a real gap where hudl_link (the actual Hudl profile-page link, required for profile completeness) was missing entirely from the mobile form (only hudl_video_link existed); verified live end-to-end including a real save
- [x] app/(tabs)/profile.tsx ← app/athlete/[slug]/page.js — already comprehensive (hero photo, circular V1 Score badge, star rating, measurable tiles, Overview/Film/Stats tabs, season stats) per earlier onboarding-artifact verification; not re-verified live this pass
- [x] app/(tabs)/match/index.tsx + [matchId].tsx ← app/dashboard/match/page.tsx — was already a comprehensive native re-skin of web's shared athlete/coach swipe page (lock states, free-tier cap, level picker, reach warning, match celebration all correctly ported from web's logic); fixed a real bug where a failed swipe (non-ok response/API error) silently advanced the card as if it saved, same class of bug web itself was patched for — added the same res.ok/data.error check + error toast; verified live
- [x] app/(tabs)/gameplan/index.tsx ← app/dashboard/gameplan/page.tsx — web just redirects into the active phase (sidebar is its own overview); mobile has no persistent sidebar so kept its explicit phase-list overview screen with progress tracker as the correct mobile-native equivalent; phase count/titles/lock logic (useGameplanPhases) match web's getGameplanPhaseStates exactly; verified live
- [x] app/(tabs)/gameplan/[phase].tsx phases 1-3 ← app/dashboard/gameplan/[phase]/page.tsx + GameplanContext.tsx — all 3 phases already built richer than web (animated score counter/tier bars on P1, sectioned form w/ completion % and Starter Bio generator on P2, same match-summary hand-off on P3); fixed a real bug: Phase 2's Hudl field read/wrote `hudl_video_link` while the phase-2-completion gate (useGameplanPhases, matching web's isPhase2Complete) checks `hudl_link` — filling in the field could never actually complete the phase; fixed to `hudl_link` everywhere in the phase-2 form; verified live. Note: an orphaned, unreachable Phase 4 ("Recruiting Timeline") exists in constants/Phases.ts + [phase].tsx but isn't linked from anywhere and isn't part of the 3-phase lock logic — left as-is, doesn't affect web parity
- [x] app/no-subscription.tsx ← visual language from app/dashboard/upgrade/page.tsx — was a generic "Access Restricted / sign in with valid account" auth-error screen (mismatched to its own filename/purpose, unreachable from anywhere currently); rewrote as an actual Match+ paywall matching upgrade.tsx's card/gradient/feature-list design; verified live

## Phase 4 — Coach (coach)
- [x] app/(coach)/index.tsx ← components/CoachDashboard.tsx — added white greeting card w/ bg photo, tier pills, View Profile link, 3 secondary action cards, full 4-stat StatCard row (added savedCount/unreadCount fetches + profile_slug field), verified live with coach QA account
- [x] app/(coach)/settings.tsx + notifications-settings.tsx ← app/coach/settings/page.tsx + notifications-settings/page.tsx — settings.tsx rewritten to match exact sections/copy (Account/Notifications/Compliance); notifications-settings.tsx was already functionally correct (same table/fields), regrouped copy to match web exactly
- [x] app/(coach)/help.tsx ← app/coach/help/page.tsx — already had correct COACH_FAQS base; fixed 3 answer strings and full 9-item subject list to match web exactly
- [x] app/(coach)/saved.tsx ← app/coach/saved/page.tsx — rebuilt to match exactly (empty state copy/icon, sort toggle, rounded-square photo, gradient V1-score badge, Remove text button); verified live (empty state, coach QA account has none saved)
- [x] app/(coach)/templates.tsx ← app/coach/templates/page.tsx — rebuilt to match exactly (Back to Recruiting link, gradient New Template button, category chips, edit/delete wired to existing update() in hook, empty-state copy); verified live incl. form open state
- [x] app/(coach)/bulk-message.tsx ← app/coach/bulk-message/page.tsx — rebuilt copy/header/cards to match web exactly; kept mobile-adapted selection (choose from Saved + Templates) since web's selection comes from a URL query param populated by Recruiting Map/Search, not yet ported; kept mobile's superior send hook (compliance check, contact-info filter, RPC send); verified live
- [x] app/(coach)/pipeline.tsx ← app/coach/pipeline/page.tsx — fixed real bug: useCoachPipeline was writing status values ('pursuing') not in the DB's pipeline_status enum (interested/contacted/visited/offered/committed/signed/declined), which would have thrown on every non-default status update; rebuilt screen with web's exact 7-status filter row + scholarship amount display; verified live
- [x] app/(coach)/recruiting.tsx ← app/coach/recruiting/page.tsx — restyled to web's white stat/insight cards, gradient circular prospect cards, position-breakdown bars w/ POSITION_COLORS; kept state-chip grid instead of web's SVG US map (better tap targets on phone width); hook already matched web's logic + the documented .is('state',...) bug fix; verified live with real coach data (6 states/12 athletes)
- [x] app/(coach)/calendar.tsx ← app/coach/calendar/page.tsx — fixed real bug: hook allowed athlete_id null but recruiting_calendar_events.athlete_id is NOT NULL (matches web's own broken hardcoded coach.id insert, which mobile does NOT replicate), so mobile now requires a saved athlete to add an event; aligned event_type values to web's exact 4 (contact/visit/game/quiet); kept mobile's month-grouped timeline over web's 7-col grid; verified live incl. no-saved-athletes form state
- [x] app/(coach)/compliance.tsx ← app/coach/compliance/page.tsx — was already a comprehensive port; added the missing full-year visual timeline bar w/ today marker and academic_year to subtitle, removed non-web eyebrow; verified live with real division data (D2, Contact Period, 84 days left)
- [x] app/(coach)/matches.tsx ← app/coach/matches/page.tsx — was already a strong port; fixed real bug where unread-count query was missing .eq('read', false) (was counting every athlete message ever, not just unread), added missing high_school to meta line; kept mobile's own working (coach)/match/[matchId] route instead of web's link to the athlete-side /dashboard/match/[id]; verified live with real match
- [x] app/(coach)/messages/index.tsx + [conversationId].tsx ← app/coach/messages/page.tsx + [id]/page.tsx — index rebuilt to match exactly (OUTREACH eyebrow, rounded-square gradient-fallback photo, position/V1 meta row, sender+date last line); thread screen already used a superior RPC-based send + correct mark-as-read, added athlete position/V1 to header subtitle; verified live
- [x] app/(coach)/profile/index.tsx + edit.tsx ← app/coach/profile/page.tsx (redirects to public [slug] profile) + edit/page.tsx — index.tsx fully rebuilt as a hero-photo profile (verified badge, stat cards, Overview/Contact tabs, Program Info grid, Recruiting Focus card) matching the public coach-profile design; edit.tsx: fixed a real bug where the "Recruiting Needs" chips wrote RECRUITING_LEVEL_BANDS values into position_needs (wrong column, min_score never came from real level_bands) — split into correct Positions You're Recruiting (position_needs) + Athlete Level (level_bands) sections, added phone_public toggle; verified live
- [x] app/(coach)/recruits/[id].tsx ← app/coach/recruits/[id]/page.tsx — was already a strong, faithful port (arguably more robust assessment query than web's); added missing "Back to Recruiting" link for consistency with sibling detail screens; verified live with real prospect data
- [x] app/(coach)/analytics.tsx ← app/coach/analytics/page.tsx — hook already computed all 6 web metrics (saved/messaged were unused); added those 2 missing KPI tiles + matched exact per-metric colors; hook is more efficient than web's N+1 loop version and correctly filters matches by status='active'; verified live with real data (7 viewed, 1 matched, 14% conversion)
- [x] app/(coach)/search.tsx ← app/coach/search/page.tsx — fixed two real bugs: a local no-op `function Image(){return null}` was shadowing the RN Image import so every prospect photo silently failed to render, and all 4 filter pills toggled a state variable that was never read anywhere (filters were completely non-functional — tapping Position/Class/State/More did nothing). Wired all 4 to real BottomSheetModal pickers (position/grad-year/state chips, min-score stepped chips, verified-only toggle); verified live end-to-end (WR filter correctly narrowed results)
- [x] app/(coach)/match/index.tsx + [matchId].tsx — was already re-skinned to match the athlete swipe deck's full-screen card language (built earlier this session, checklist just hadn't been marked); added the same swipe-error-toast fix as athlete match/index.tsx (silently-swallowed failed swipes); verified live with real athlete card
- [x] app/coach-setup.tsx — confirmed still the intentional WebView-of-live-web-page pattern (session cookie injection, byte-for-byte same wizard as web); no rebuild needed

## Phase 5 — Cleanup
- [x] ui/ListRow.tsx and ui/SmoothLineChart.tsx had zero importers — deleted both. ui/Card, ui/EmptyState, and ui/BottomSheetModal are still legitimately imported (6, 6, 2 files respectively) as genuine shared primitives across many screens built/rebuilt this session — kept, this supersedes the original plan's assumption that these would reach zero
- [x] Full closing pass: logged in fresh via the real UI (not deep-link-while-wrong-role) as both QA accounts. Coach account: every single drawer item clicked and screenshot-verified with zero crashes/blank screens — Dashboard, Discover, Recruit Search, Saved, My Matches, Pipeline, Recruiting Board, Messages, Bulk Message, Templates, Calendar, Analytics, Compliance, Settings, Notification Preferences, Help, Profile, Profile Edit. Athlete account: drawer confirmed complete and correctly populated with real data (Kobee Bolton, Match+ tier); each athlete screen was already individually screenshot-verified live as it was built earlier in this session
